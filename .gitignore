node_modules/
.next/
dist/
coverage/
playwright-report/
test-results/
.env
.env.*
!.env.example
*.tsbuildinfo


# Supabase CLI scratch (version cache, linked project state)
supabase/.temp

# GitHub App private keys must never enter the repo, in any form.
*.pem

# CI test reports at the root (the workflow uploads them; nothing reads them
# from the tree). Anchored: docs/reports/ is tracked.
/reports/
