# todo 11·12·13 — 코칭 · 기여도+VIBE · 지표 주입 A/B (2026-08-17)

**범위:** BUILD_PLAN_PHASE2B Wave 4 후반부. 세 커밋: `feat(teams): score and coach prompts`, `feat(teams): evidence-based contribution and vibe index`, `feat(bench): validate vibe metrics against hidden tests`.

## 1. todo 11 — 프롬프트 코칭 (`packages/core/src/team/prompt-coach.ts`)

- 루브릭 6축 0–2점 strict 스키마(컨텍스트 근거·구체성·검증 가능성·배치 크기·정지 조건·과잉 지시 없음), 출력 `grade`는 **리터럴 `inferred`**(다른 값은 스키마가 거부).
- **결정론 바닥 규칙(anti-shell):** 프롬프트 자체에서 관측 가능한 신호(검증 신호·컨텍스트 인용·정지 조건·길이)로 축별 상한을 계산 — 상한 초과 점수는 clamp가 아니라 **무효**(관측 안 되는 근거를 주장하는 채점기는 신뢰·과금 대상이 아님). 신호 없는 축은 개선안 제안으로 변환("검증 신호를 넣으세요 — 채택 확률 ~8배").
- **무과금 계약:** provider 실패·스키마 위반·바닥 위반 전부 `CoachingValidationError` → `coachingCreditCost` = 0. 성공만 1.
- **로컬 전용 사용자:** 순수 함수(프롬프트 텍스트 + 주입 provider) — 워크스페이스·서버·동의 기계 없이 로컬 프롬프트 로그에서 직접 코칭(테스트로 증명).

수용 기준 ↔ `tests/prompt-coach.test.ts` 6케이스: 스키마 계약(등급·범위·잉여 필드·제안 상한), 껍데기 프롬프트 고득점 무효(음성), 실패 3경로 무과금, 로컬 경로.

## 2. todo 12 — 기여도 + VIBE v0 (`packages/core/src/team/vibe-index.ts`)

- **자가보고 구조적 불가:** 입력은 strict zod(commits·receipts·resolvedFindings·provenRequirements·promptRecords — 전부 로그 파생). `selfReported*` 류 잉여 필드는 어느 층에서든 파싱 거부(테스트가 스키마 키 목록 자체를 고정).
- 후보 V1~V7 결정론 산식: verified 증거 비율 / 발견 해소율 / 요구사항 증명 처리량 / 루브릭 평균 / receipt 연속성 / verified commit 비율 / 검증 가능성 만점 비율.
- **이중 게이트:** ⑴ Goodhart — `benchmarks/vibe/gate-results.json`에서 `adopted`인 지표만 personal/teamView에 렌더(ADR-011-7). **커밋된 게이트 파일은 전부 pending → 현재 아무 지표도 렌더되지 않음을 게시 파일 기준으로 테스트.** ⑵ 비교 표 — 정책 미활성 시 `comparisonTable === null`(빈 배열이 아니라 부재).
- 팀 뷰는 집계(평균)만, 개인 행은 본인용. 기여도 행 = "누가 어떤 요구사항을 증명까지"(commit→receipt·proven requirement 연결).

수용 기준 ↔ `tests/vibe-index.test.ts` 6케이스: 산식 결정론(재실행 동일), 자가보고 부재 증명, 비교 표 정책 게이트, 미채택 지표 비노출(1개 채택/0개 채택), **게시 파일 연동**, 기여도 행.

## 3. todo 13 — 지표 하네스 주입 A/B (`scripts/vibe-injection-experiment.ts`)

- **실행 가능한 실험**: 동결 v3 매니페스트에서 모델 2종(2 provider)과 숨긴 정답 과제(픽스처 answer-manifest 4종)를 읽어 **지표 7 × 하네스 2(control/injected) × 모델 2 × 과제 4 = 112시행 그리드**를 사전등록하고, 지표별 주입 지시문으로 실제 컨텍스트 조립까지 수행(파이프라인 실증).
- **판정 기록 공개:** `benchmarks/vibe/gate-results.json` + `vibe-injection.dry-run.md` 커밋 — 실모델 크레딧 대기이므로 전 지표 **pending**(v3 사전등록과 같은 정직성 프로토콜 — 결과 조작 없이 상태 그대로 게시). 채택 규칙 명문: **지표↑ AND 숨긴 정답 정확도↑**만 채택, 지표만 오르면 폐기.
- **자동 비노출 배선:** todo 12의 노출 게이트가 이 게시 파일을 읽는다 — pending/rejected는 제품 어디에도 렌더 불가(테스트 상호 연동).

수용 기준 ↔ `tests/vibe-injection.test.ts` 4케이스: 그리드 전수(지표×하네스×모델 균형), 지표별 지시문 유일성, pending 판정(조작 없음), 게시 파일 스키마·todo 12 연동.

## 4. 게이트

- vitest **642/643** (87 파일; 신규 16) · eslint·typecheck 무결점 · 스코프 스캐너 PASS(208)
- Playwright 미실행 — 웹 UI 무변경(core·scripts·게시 파일만). VIBE/코칭 화면이 생기는 시점에 e2e 편입.

## 5. 남긴 것

- 실모델 실행(112시행)은 크레딧 해제 시 — 게이트 파일이 adopted/rejected로 갱신되면 todo 12 노출이 자동으로 따라온다.
- 코칭·VIBE의 화면/MCP 배선(코칭 판단 잡의 크레딧 원장 연결 포함)은 후속.
